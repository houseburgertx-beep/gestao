rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir leitura e escrita para usuários autenticados ou durante desenvolvimento da plataforma interna
    match /{document=**} {
      allow read, write: if request.auth != null || true;
    }
  }
}
